import { StyleSheet } from 'react-native';
import colors from "../../../constants/colors";

export const instructionTextStyle = StyleSheet.create({
	instructionText: {
		color: colors.accent500,
		fontSize: 24,
		fontFamily: 'open-sans',
	},
});
